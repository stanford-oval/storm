# FastAPI and server dependencies
fastapi==0.109.2
uvicorn[standard]==0.24.0

# Data processing and validation
pydantic==2.5.0
python-frontmatter==1.0.0

# Date and time utilities
python-dateutil==2.8.2

# File handling and utilities
aiofiles==23.2.1
pathlib2==2.3.7.post1

# Authentication and security
PyJWT==2.10.1  # Secure JWT implementation (Updated to fix CVE-2024-53861)
cryptography==44.0.1  # For JWT signing (Updated to fix OpenSSL vulnerabilities)
passlib[bcrypt]==1.7.4
python-multipart==0.0.18  # Updated to fix DoS vulnerability (CVE-2024-53981)

# STORM dependencies (assumed to be installed separately)
# knowledge-storm should be installed from the parent directory
# pip install -e ../

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
black==24.3.0  # Updated to fix ReDoS vulnerability (CVE-2024-21503)
isort==5.12.0
mypy==1.7.1

# Type stubs for mypy
types-requests==2.31.0.10
types-toml==0.10.8.7
types-pytz==2024.1.0.20240203

# Additional utilities
typing-extensions>=4.11,<5
# Export functionality
markdown2>=2.5.4  # For HTML export (Updated to fix XSS and ReDoS vulnerabilities)
